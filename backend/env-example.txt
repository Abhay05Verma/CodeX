# MongoDB (local by default)
MONGODB_URI=mongodb://localhost:27017/codex

# Auth secret for upcoming protected routes
JWT_SECRET=replace-this-with-a-long-random-secret

# Server config
PORT=3000
NODE_ENV=development

# Frontend origin (CORS) – set to your deployed frontend URL or http://localhost:3001 for local
CLIENT_URL=http://localhost:3001

# Optional: Loan request email notifications (leave unset to log only)
# FRONTEND_URL=http://localhost:3001
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@example.com
